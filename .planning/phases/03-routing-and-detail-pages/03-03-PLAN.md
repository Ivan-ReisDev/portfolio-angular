---
phase: 03-routing-and-detail-pages
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/app/pages/project-detail/project-detail.ts
  - src/app/pages/project-detail/project-detail.html
  - src/app/pages/project-detail/project-detail.scss
  - src/app/core/components/project-card/project-card.html
  - src/app/core/components/project-card/project-card.ts
autonomous: true

must_haves:
  truths:
    - "Usuario acessa /projeto/sistema-gestao e ve pagina completa do projeto"
    - "Pagina mostra titulo, descricao completa, tecnologias, features e galeria"
    - "Usuario pode navegar para projeto anterior/proximo no rodape"
    - "Clicar em card no carousel navega para pagina de detalhe"
    - "Botao voltar retorna para secao Projects da home"
    - "Demo iframe funciona quando projeto tem campo iframe"
  artifacts:
    - path: "src/app/pages/project-detail/project-detail.ts"
      provides: "Pagina de detalhe de projeto"
      exports: ["ProjectDetail"]
    - path: "src/app/pages/project-detail/project-detail.html"
      provides: "Template com layout duas colunas"
      contains: "app-image-gallery"
    - path: "src/app/core/components/project-card/project-card.html"
      provides: "Link de navegacao para detalhe"
      contains: "routerLink"
  key_links:
    - from: "project-detail.ts"
      to: "ProjectService"
      via: "inject para buscar projeto por slug"
      pattern: "projectService.*projects"
    - from: "project-detail.ts"
      to: "ImageGallery"
      via: "import e uso no template"
      pattern: "ImageGallery"
    - from: "project-card.html"
      to: "/projeto/:slug"
      via: "routerLink para navegacao"
      pattern: "routerLink.*projeto"
---

<objective>
Criar pagina ProjectDetail e conectar navegacao do carousel

Purpose: Permitir que usuarios visualizem informacoes completas de cada projeto em uma pagina dedicada, com galeria de imagens, lista de features, e navegacao entre projetos.

Output: Pagina de detalhe completa com layout duas colunas, integracao com ImageGallery, navegacao prev/next, e cards do carousel clicaveis.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-routing-and-detail-pages/03-CONTEXT.md
@.planning/phases/03-routing-and-detail-pages/03-RESEARCH.md
@src/app/core/services/project.service.ts
@src/app/core/models/project.model.ts
@src/app/core/components/project-card/project-card.ts
@src/app/core/components/project-card/project-card.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar componente ProjectDetail com layout duas colunas</name>
  <files>
src/app/pages/project-detail/project-detail.ts
src/app/pages/project-detail/project-detail.html
src/app/pages/project-detail/project-detail.scss
  </files>
  <action>
Criar componente ProjectDetail standalone:

**TypeScript (project-detail.ts):**
```typescript
import { Component, input, computed, inject } from '@angular/core';
import { Router, RouterLink, ActivatedRoute } from '@angular/router';
import { ProjectService } from '../../core/services/project.service';
import { ImageGallery } from '../../core/components/image-gallery/image-gallery';

@Component({
  selector: 'app-project-detail',
  standalone: true,
  imports: [RouterLink, ImageGallery],
  templateUrl: './project-detail.html',
  styleUrl: './project-detail.scss'
})
export class ProjectDetail {
  private readonly projectService = inject(ProjectService);
  private readonly router = inject(Router);
  private readonly route = inject(ActivatedRoute);

  // Route param bound via withComponentInputBinding
  slug = input<string>('');

  // Fallback para deep link (bug conhecido Angular 19+)
  readonly effectiveSlug = computed(() =>
    this.slug() || this.route.snapshot.paramMap.get('slug') || ''
  );

  readonly project = computed(() => {
    const s = this.effectiveSlug();
    return this.projectService.projects().find(p => p.id === s);
  });

  readonly projectIndex = computed(() => {
    const projects = this.projectService.projects();
    return projects.findIndex(p => p.id === this.effectiveSlug());
  });

  // Navegacao circular
  readonly prevProject = computed(() => {
    const projects = this.projectService.projects();
    const idx = this.projectIndex();
    if (idx === -1 || projects.length <= 1) return null;
    const prevIdx = idx === 0 ? projects.length - 1 : idx - 1;
    return projects[prevIdx];
  });

  readonly nextProject = computed(() => {
    const projects = this.projectService.projects();
    const idx = this.projectIndex();
    if (idx === -1 || projects.length <= 1) return null;
    const nextIdx = idx === projects.length - 1 ? 0 : idx + 1;
    return projects[nextIdx];
  });

  goBack(): void {
    this.router.navigate(['/'], { fragment: 'projetos' });
  }
}
```

**Template (project-detail.html):**
Layout de duas colunas (desktop) que empilha no mobile:

```html
@if (project(); as proj) {
  <main class="project-detail">
    <header class="detail-header">
      <button class="back-btn" (click)="goBack()">
        <i class="fa-solid fa-arrow-left"></i>
        Voltar aos Projetos
      </button>
      <h1 class="title">{{ proj.title }}</h1>
    </header>

    <div class="detail-content">
      <!-- Coluna esquerda: Galeria -->
      <section class="gallery-column">
        <app-image-gallery
          [images]="proj.images"
          [alt]="proj.title" />

        @if (proj.iframe) {
          <div class="demo-section">
            <button class="demo-btn" (click)="openDemo()">
              <i class="fa-solid fa-play"></i>
              Ver Demo Interativa
            </button>
          </div>
        }
      </section>

      <!-- Coluna direita: Info -->
      <section class="info-column">
        <div class="description">
          <h2>Sobre o Projeto</h2>
          <p>{{ proj.fullDescription }}</p>
        </div>

        <div class="technologies">
          <h2>Tecnologias</h2>
          <div class="tech-tags">
            @for (tech of proj.technologies; track tech) {
              <span class="tech-tag">
                <i [class]="'devicon-' + tech.toLowerCase() + '-plain'"></i>
                {{ tech }}
              </span>
            }
          </div>
        </div>

        <div class="features">
          <h2>Funcionalidades</h2>
          <ul>
            @for (feature of proj.features; track feature) {
              <li>{{ feature }}</li>
            }
          </ul>
        </div>

        <div class="links">
          @if (proj.demoUrl) {
            <a [href]="proj.demoUrl" target="_blank" class="link-btn demo">
              <i class="fa-solid fa-external-link-alt"></i>
              Ver Demo
            </a>
          }
          @if (proj.githubUrl) {
            <a [href]="proj.githubUrl" target="_blank" class="link-btn github">
              <i class="fa-brands fa-github"></i>
              Ver Codigo
            </a>
          }
        </div>
      </section>
    </div>

    <!-- Navegacao prev/next -->
    <nav class="project-nav">
      @if (prevProject(); as prev) {
        <a [routerLink]="['/projeto', prev.id]" class="nav-link prev">
          <i class="fa-solid fa-chevron-left"></i>
          <div class="nav-content">
            <span class="nav-label">Anterior</span>
            <span class="nav-title">{{ prev.title }}</span>
          </div>
          <img [src]="prev.images[0]" [alt]="prev.title" class="nav-thumb" />
        </a>
      }
      @if (nextProject(); as next) {
        <a [routerLink]="['/projeto', next.id]" class="nav-link next">
          <img [src]="next.images[0]" [alt]="next.title" class="nav-thumb" />
          <div class="nav-content">
            <span class="nav-label">Proximo</span>
            <span class="nav-title">{{ next.title }}</span>
          </div>
          <i class="fa-solid fa-chevron-right"></i>
        </a>
      }
    </nav>
  </main>
} @else {
  <div class="not-found">
    <h1>Projeto nao encontrado</h1>
    <button (click)="goBack()">Voltar aos Projetos</button>
  </div>
}
```

**Estilos (project-detail.scss):**
- Background: $primary-black com gradiente sutil
- Layout: CSS Grid duas colunas no desktop (60% galeria, 40% info), stack no mobile
- Header: padding generoso, botao voltar estilizado
- Galeria: ocupa altura proporcional
- Info: glassmorphism card, padding interno
- Tech tags: estilo consistente com ProjectCard
- Features list: bullets customizados
- Links: botoes com hover effect, cores distintas (demo azul, github cinza)
- Nav prev/next: grid duas colunas, thumbnail + titulo, hover scale
- Breakpoint: 768px para stack mobile (info primeiro, galeria depois)
- Transicoes suaves em todos elementos interativos
  </action>
  <verify>
Componente compila. Acessar /projeto/sistema-gestao mostra pagina completa. Layout responsivo funciona.
  </verify>
  <done>
ProjectDetail exibe todas informacoes do projeto, galeria funcional, navegacao prev/next, e botao voltar.
  </done>
</task>

<task type="auto">
  <name>Task 2: Adicionar navegacao de click nos cards do carousel</name>
  <files>
src/app/core/components/project-card/project-card.ts
src/app/core/components/project-card/project-card.html
  </files>
  <action>
Atualizar ProjectCard para navegar ao clicar:

**project-card.ts:**
Adicionar RouterLink aos imports do componente.

**project-card.html:**
Envolver o card em um link ou adicionar routerLink no container:

```html
<a [routerLink]="['/projeto', project().id]" class="card-link">
  <!-- conteudo existente do card -->
</a>
```

OU adicionar apenas na area clicavel (nao nos botoes de link):

```html
<article class="project-card" [class.active]="isActive()">
  <a [routerLink]="['/projeto', project().id]" class="card-image-link">
    <img ... />
  </a>
  <!-- resto do card -->
</article>
```

IMPORTANTE:
- Nao quebrar funcionalidade existente dos links externos (demoUrl, githubUrl)
- Manter estilos e hover effects existentes
- O card inteiro (exceto botoes externos) deve ser clicavel

Adicionar estilo para cursor pointer na area clicavel se necessario.
  </action>
  <verify>
Clicar em card do carousel navega para /projeto/{id}. Links externos (demo, github) ainda funcionam separadamente.
  </verify>
  <done>
Cards do carousel navegam para pagina de detalhe ao clicar. Links externos preservados.
  </done>
</task>

<task type="auto">
  <name>Task 3: Adicionar suporte a iframe de demo (modal opcional)</name>
  <files>
src/app/pages/project-detail/project-detail.ts
src/app/pages/project-detail/project-detail.html
src/app/pages/project-detail/project-detail.scss
  </files>
  <action>
Adicionar funcionalidade de demo interativa para projetos que tem campo `iframe`:

**TypeScript:**
Adicionar signal e metodos para controlar modal de demo:

```typescript
demoOpen = signal(false);

openDemo(): void {
  this.demoOpen.set(true);
  document.body.style.overflow = 'hidden';
}

closeDemo(): void {
  this.demoOpen.set(false);
  document.body.style.overflow = '';
}
```

Adicionar HostListener para ESC fechar demo.

**Template:**
Adicionar modal de demo no final do template:

```html
@if (demoOpen() && project()?.iframe) {
  <div class="demo-modal" (click)="closeDemo()">
    <div class="demo-container" (click)="$event.stopPropagation()">
      <button class="close-demo" (click)="closeDemo()">
        <i class="fa-solid fa-times"></i>
      </button>
      <iframe
        [src]="project()!.iframe"
        title="Demo interativa"
        allowfullscreen>
      </iframe>
    </div>
  </div>
}
```

**Estilos:**
- Modal: fullscreen overlay, z-index alto
- Container: 90vw x 80vh, centralizado
- Iframe: 100% width/height, border-radius
- Botao fechar: absoluto no canto superior direito
- Animacao de entrada (fade + scale)

NOTA: Projetos sem campo `iframe` nao mostram botao "Ver Demo Interativa".
  </action>
  <verify>
Projeto chatbot-ai (que tem iframe) mostra botao e abre modal. Outros projetos nao mostram botao. ESC fecha modal.
  </verify>
  <done>
Modal de demo funciona para projetos com iframe. ESC e clique fora fecham. Body scroll travado quando aberto.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` compila sem erros
2. Build gera paginas prerenderizadas em /projeto/{slug} para cada projeto
3. Navegar para /projeto/sistema-gestao mostra pagina completa
4. Galeria funciona com thumbnails e lightbox
5. Navegacao prev/next funciona em loop circular
6. Cards do carousel navegam para pagina de detalhe
7. Botao voltar retorna para secao #projetos da home
8. Modal de demo funciona para projeto com iframe (chatbot-ai)
</verification>

<success_criteria>
- Cada projeto tem URL dedicada acessivel
- Pagina mostra todas informacoes: titulo, descricao completa, tecnologias, features, galeria
- Navegacao prev/next funciona com loop circular
- Cards do carousel sao clicaveis e navegam para detalhe
- Demo iframe funciona quando projeto tem campo configurado
- Layout responsivo: duas colunas desktop, stack mobile
- Visual consistente com tema glassmorphism do portfolio
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/03-routing-and-detail-pages/03-03-SUMMARY.md`
</output>
