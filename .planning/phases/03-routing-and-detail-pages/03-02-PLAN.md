---
phase: 03-routing-and-detail-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/core/components/image-gallery/image-gallery.ts
  - src/app/core/components/image-gallery/image-gallery.html
  - src/app/core/components/image-gallery/image-gallery.scss
  - src/app/core/components/lightbox/lightbox.ts
  - src/app/core/components/lightbox/lightbox.html
  - src/app/core/components/lightbox/lightbox.scss
autonomous: true

must_haves:
  truths:
    - "Usuario pode ver imagem principal grande"
    - "Usuario pode clicar em thumbnails para trocar imagem principal"
    - "Usuario pode abrir lightbox ao clicar na imagem principal"
    - "Usuario pode navegar entre imagens no lightbox (setas + swipe + teclado)"
    - "Usuario pode fechar lightbox com ESC, clique fora ou botao X"
    - "Lightbox mostra contador de posicao (ex: 2 de 5)"
  artifacts:
    - path: "src/app/core/components/image-gallery/image-gallery.ts"
      provides: "Componente de galeria com thumbnails"
      exports: ["ImageGallery"]
    - path: "src/app/core/components/lightbox/lightbox.ts"
      provides: "Overlay fullscreen para imagens"
      exports: ["Lightbox"]
  key_links:
    - from: "image-gallery.ts"
      to: "lightbox.ts"
      via: "event output para abrir lightbox"
      pattern: "openLightbox.*EventEmitter"
    - from: "lightbox.ts"
      to: "SwipeDirective"
      via: "directive para navegacao por swipe"
      pattern: "appSwipe"
---

<objective>
Criar componentes ImageGallery e Lightbox para exibicao de screenshots de projetos

Purpose: Permitir que usuarios visualizem multiplas imagens de cada projeto, com galeria interativa e lightbox fullscreen para visualizacao detalhada.

Output: Dois componentes reutilizaveis - ImageGallery (imagem principal + thumbnails) e Lightbox (overlay fullscreen com navegacao).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-routing-and-detail-pages/03-CONTEXT.md
@.planning/phases/03-routing-and-detail-pages/03-RESEARCH.md
@src/app/core/directives/swipe.directive.ts
@src/styles/_variables.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar componente Lightbox com CDK overlay patterns</name>
  <files>
src/app/core/components/lightbox/lightbox.ts
src/app/core/components/lightbox/lightbox.html
src/app/core/components/lightbox/lightbox.scss
  </files>
  <action>
Criar componente Lightbox standalone com:

**TypeScript (lightbox.ts):**
```typescript
import { Component, input, output, signal, computed, HostListener, PLATFORM_ID, inject } from '@angular/core';
import { isPlatformBrowser } from '@angular/common';
import { SwipeDirective } from '../../directives/swipe.directive';

@Component({
  selector: 'app-lightbox',
  standalone: true,
  imports: [SwipeDirective],
  templateUrl: './lightbox.html',
  styleUrl: './lightbox.scss'
})
export class Lightbox {
  images = input.required<string[]>();
  initialIndex = input<number>(0);
  alt = input<string>('Imagem do projeto');

  closed = output<void>();

  private readonly platformId = inject(PLATFORM_ID);

  currentIndex = signal(0);

  readonly currentImage = computed(() => this.images()[this.currentIndex()]);
  readonly total = computed(() => this.images().length);
  readonly counter = computed(() => `${this.currentIndex() + 1} de ${this.total()}`);
  readonly hasPrev = computed(() => this.total() > 1);
  readonly hasNext = computed(() => this.total() > 1);

  ngOnInit() {
    this.currentIndex.set(this.initialIndex());
    if (isPlatformBrowser(this.platformId)) {
      document.body.style.overflow = 'hidden';
    }
  }

  ngOnDestroy() {
    if (isPlatformBrowser(this.platformId)) {
      document.body.style.overflow = '';
    }
  }

  @HostListener('document:keydown.escape')
  onEscape() { this.close(); }

  @HostListener('document:keydown.arrowleft')
  onArrowLeft() { this.prev(); }

  @HostListener('document:keydown.arrowright')
  onArrowRight() { this.next(); }

  close() {
    this.closed.emit();
  }

  prev() {
    const total = this.total();
    if (total <= 1) return;
    const newIndex = this.currentIndex() === 0 ? total - 1 : this.currentIndex() - 1;
    this.currentIndex.set(newIndex);
  }

  next() {
    const total = this.total();
    if (total <= 1) return;
    const newIndex = this.currentIndex() === total - 1 ? 0 : this.currentIndex() + 1;
    this.currentIndex.set(newIndex);
  }
}
```

**Template (lightbox.html):**
- Backdrop escuro clicavel para fechar
- Imagem centralizada com max-width/max-height para caber na tela
- Botao X no canto superior direito
- Setas de navegacao nas laterais (ocultas se apenas 1 imagem)
- Contador no rodape
- appSwipe directive para navegacao por swipe

**Estilos (lightbox.scss):**
- Backdrop: position fixed, inset 0, background rgba(0,0,0,0.9), z-index 1000
- Imagem: max-width 90vw, max-height 85vh, object-fit contain
- Botao fechar: posicao absoluta top-right, estilo consistente com tema
- Setas: nas laterais, vertical center, hover effect
- Contador: centralizado no rodape, cor clara sobre fundo escuro
- Animacao de fade-in ao abrir
  </action>
  <verify>
Componente compila sem erros. Template usa @if para condicional das setas. Keyboard listeners registrados corretamente.
  </verify>
  <done>
Lightbox funciona com: ESC fecha, setas teclado navegam, swipe navega, clique fora fecha, contador mostra posicao.
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar componente ImageGallery com thumbnails</name>
  <files>
src/app/core/components/image-gallery/image-gallery.ts
src/app/core/components/image-gallery/image-gallery.html
src/app/core/components/image-gallery/image-gallery.scss
  </files>
  <action>
Criar componente ImageGallery standalone com:

**TypeScript (image-gallery.ts):**
```typescript
import { Component, input, signal, computed } from '@angular/core';
import { Lightbox } from '../lightbox/lightbox';

@Component({
  selector: 'app-image-gallery',
  standalone: true,
  imports: [Lightbox],
  templateUrl: './image-gallery.html',
  styleUrl: './image-gallery.scss'
})
export class ImageGallery {
  images = input.required<string[]>();
  alt = input<string>('Imagem do projeto');

  currentIndex = signal(0);
  lightboxOpen = signal(false);

  readonly currentImage = computed(() => this.images()[this.currentIndex()]);
  readonly hasMultipleImages = computed(() => this.images().length > 1);

  selectImage(index: number) {
    this.currentIndex.set(index);
  }

  openLightbox() {
    this.lightboxOpen.set(true);
  }

  closeLightbox() {
    this.lightboxOpen.set(false);
  }
}
```

**Template (image-gallery.html):**
- Container principal com imagem grande
- Imagem principal clicavel (abre lightbox)
- Container de thumbnails abaixo (se mais de 1 imagem)
- Cada thumbnail e um button com classe .active quando selecionado
- Lightbox renderizado condicionalmente (@if lightboxOpen())

**Estilos (image-gallery.scss):**
- Imagem principal: width 100%, aspect-ratio 16/10, object-fit cover, border-radius, cursor pointer
- Container thumbnails: display flex, gap, overflow-x auto para scroll horizontal se necessario
- Thumbnails: tamanho fixo (80x50px), border quando ativo, hover effect, cursor pointer
- Usar variaveis SCSS do projeto ($primary-black, $secondary-blue, etc)
- Glassmorphism sutil na borda da imagem principal (consistente com tema)
  </action>
  <verify>
Componente compila sem erros. Thumbnails mostram indicador de selecao. Lightbox abre/fecha corretamente.
  </verify>
  <done>
ImageGallery exibe imagem principal, thumbnails navegaveis, e abre Lightbox ao clicar na imagem principal.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` compila sem erros
2. Ambos componentes sao standalone e exportam corretamente
3. Lightbox trava scroll do body quando aberto
4. SwipeDirective integrado no Lightbox para navegacao touch
5. Estilos seguem tema escuro do portfolio
</verification>

<success_criteria>
- ImageGallery mostra imagem principal + thumbnails clicaveis
- Lightbox abre fullscreen com backdrop escuro
- Navegacao funciona: setas, teclado (ESC, arrows), swipe
- Contador mostra posicao atual ("2 de 5")
- Body scroll travado quando lightbox aberto
- Visual consistente com glassmorphism e tema escuro do portfolio
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/03-routing-and-detail-pages/03-02-SUMMARY.md`
</output>
