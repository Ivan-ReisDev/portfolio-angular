---
phase: 03-routing-and-detail-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/app.config.ts
  - src/app/app.routes.ts
  - src/app/app.routes.server.ts
autonomous: true

must_haves:
  truths:
    - "Rota /projeto/:slug existe e pode ser acessada"
    - "Paginas de projeto sao pre-renderizadas no build"
    - "View transitions funcionam ao navegar entre paginas"
    - "Scroll position e restaurado ao voltar"
  artifacts:
    - path: "src/app/app.routes.ts"
      provides: "Rota dinamica para projeto"
      contains: "projeto/:slug"
    - path: "src/app/app.routes.server.ts"
      provides: "Configuracao de prerendering"
      contains: "getPrerenderParams"
    - path: "src/app/app.config.ts"
      provides: "Features de router"
      contains: "withComponentInputBinding"
  key_links:
    - from: "app.routes.ts"
      to: "pages/project-detail"
      via: "loadComponent lazy loading"
      pattern: "loadComponent.*project-detail"
    - from: "app.routes.server.ts"
      to: "public/data/projects.json"
      via: "fs.readFile para getPrerenderParams"
      pattern: "projects\\.json"
---

<objective>
Configurar roteamento Angular para paginas de projeto com prerendering SSR

Purpose: Estabelecer a infraestrutura de rotas que permitira URLs individuais para cada projeto, com suporte a prerendering estatico e transicoes suaves entre paginas.

Output: Configuracao completa de rotas cliente e servidor, habilitando lazy loading da pagina de detalhe e pre-geracao de todas as paginas de projeto no build.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-routing-and-detail-pages/03-CONTEXT.md
@.planning/phases/03-routing-and-detail-pages/03-RESEARCH.md
@src/app/app.config.ts
@src/app/app.routes.ts
@src/app/app.routes.server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configurar features do router em app.config.ts</name>
  <files>src/app/app.config.ts</files>
  <action>
Atualizar provideRouter() para incluir as seguintes features:

1. `withComponentInputBinding()` - Permite binding de parametros de rota para inputs de componentes (slug -> input signal)
2. `withViewTransitions()` - Habilita View Transitions API para transicoes suaves entre paginas
3. `withInMemoryScrolling({ scrollPositionRestoration: 'enabled', anchorScrolling: 'enabled' })` - Restaura posicao de scroll ao navegar e suporta ancoras (#projetos)

Imports necessarios de @angular/router:
- withComponentInputBinding
- withViewTransitions
- withInMemoryScrolling

Manter providers existentes (provideBrowserGlobalErrorListeners, provideClientHydration, provideHttpClient).
  </action>
  <verify>
`npm run build` compila sem erros. Verificar que app.config.ts contem os tres features adicionados.
  </verify>
  <done>
app.config.ts tem provideRouter com withComponentInputBinding, withViewTransitions e withInMemoryScrolling configurados.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configurar rota dinamica em app.routes.ts</name>
  <files>src/app/app.routes.ts</files>
  <action>
Adicionar rota para pagina de detalhe de projeto:

```typescript
{
  path: 'projeto/:slug',
  loadComponent: () => import('./pages/project-detail/project-detail')
    .then(m => m.ProjectDetail),
  title: 'Projeto'
}
```

A rota usa lazy loading para carregar o componente apenas quando acessado. O parametro :slug sera mapeado para o input do componente via withComponentInputBinding.

Nao modificar a rota raiz (path: '') - ela continua usando o app component padrao.

NOTA: O componente ProjectDetail ainda nao existe - sera criado no Plan 03. A rota funcionara apos criacao do componente.
  </action>
  <verify>
Arquivo contem a rota 'projeto/:slug' com loadComponent apontando para project-detail.
  </verify>
  <done>
app.routes.ts tem rota /projeto/:slug com lazy loading configurado.
  </done>
</task>

<task type="auto">
  <name>Task 3: Configurar prerendering com getPrerenderParams</name>
  <files>src/app/app.routes.server.ts</files>
  <action>
Atualizar serverRoutes para incluir prerendering da rota de projeto:

```typescript
import { RenderMode, ServerRoute, PrerenderFallback } from '@angular/ssr';

export const serverRoutes: ServerRoute[] = [
  {
    path: '',
    renderMode: RenderMode.Prerender
  },
  {
    path: 'projeto/:slug',
    renderMode: RenderMode.Prerender,
    async getPrerenderParams() {
      // Ler IDs de projeto diretamente do filesystem no build time
      const fs = await import('node:fs/promises');
      const path = await import('node:path');
      const filePath = path.join(process.cwd(), 'public', 'data', 'projects.json');
      const data = JSON.parse(await fs.readFile(filePath, 'utf-8'));
      return data.projects.map((p: { id: string }) => ({ slug: p.id }));
    },
    fallback: PrerenderFallback.Server
  },
  {
    path: '**',
    renderMode: RenderMode.Prerender
  }
];
```

IMPORTANTE:
- Usar node:fs/promises (nao HttpClient) porque getPrerenderParams roda no build time
- PrerenderFallback.Server garante SSR para slugs desconhecidos
- A ordem das rotas importa: rota especifica antes do wildcard **
  </action>
  <verify>
`npm run build` executa sem erros. Verificar no output do build que as paginas de projeto sao geradas (deve listar /projeto/sistema-gestao, /projeto/ecommerce-platform, etc).
  </verify>
  <done>
app.routes.server.ts tem getPrerenderParams que le projects.json e retorna array de slugs para prerendering.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completa sem erros
2. Build output lista rotas prerenderizadas incluindo /projeto/:slug para cada projeto
3. Imports em app.config.ts estao corretos (sem warnings de compilacao)
</verification>

<success_criteria>
- Rota /projeto/:slug configurada com lazy loading
- Features de router (view transitions, scroll restoration, input binding) habilitados
- Prerendering configurado para gerar paginas estaticas de todos os projetos no build
- Build compila sem erros (componente ainda nao existe, mas rota esta preparada)
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/03-routing-and-detail-pages/03-01-SUMMARY.md`
</output>
